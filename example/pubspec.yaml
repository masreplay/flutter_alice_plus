name: alice_plus_example
description: Demonstrates how to use the Alice plugin.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  dio: ^5.1.1
  chopper: ^8.0.3
  overlay_support: ^2.1.0
  share_plus: ^10.1.4
  rxdart: ^0.28.0

dev_dependencies:
  build_runner: ^2.0.6
  chopper_generator: ^8.0.3
  flutter_alice_plus:
    path: ../

flutter:
  uses-material-design: true
